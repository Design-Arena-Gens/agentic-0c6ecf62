# BrandFlow Studio

مساعد إبداعي يعمل بالذكاء الاصطناعي لتصميم محتوى السوشيال ميديا، ابتكار الشعارات، بناء الهوية البصرية، وإعداد فيديوهات الإطلاق من خلال تجربة محادثة واحدة.

## ✨ المزايا الرئيسية

- مدير إبداعي افتراضي يتحدث العربية والإنجليزية.
- قوالب سريعة لخطة محتوى، هوية بصرية، شعار، وفيديو موشن.
- توصيات تفصيلية للأحجام، الصياغة، والأدوات المستخدمة في التنفيذ.
- مخرجات منظمة مع خطوات تالية واضحة قابلة للتنفيذ فوراً.

## 🚀 التشغيل محلياً

```bash
npm install
npm run dev
```

ثم افتح المتصفح على [http://localhost:3000](http://localhost:3000).

## 🔐 الإعدادات المطلوبة

أنشئ ملف بيئة وعرّف مفتاح OpenAI:

```bash
cp .env.example .env.local
```

```
OPENAI_API_KEY=sk-xxx
```

> في حال عدم توافر المفتاح سيعرض التطبيق رسالة توضح ضرورة الإعداد.

## 🧠 كيف يعمل BrandFlow

- يعتمد على [Vercel AI SDK](https://sdk.vercel.ai) ونموذج `gpt-4o-mini`.
- واجهة المحادثة تستخدم `useChat` مع بث مباشر للردود.
- مسار العمل مهيأ لإنتاج خطط تصميم، أدلة هوية، وسيناريوهات فيديو قابلة للتنفيذ.

## 🧪 أوامر إضافية

```bash
npm run lint     # التحقق من جودة الكود
npm run build    # بناء نسخة الإنتاج
npm start        # تشغيل نسخة الإنتاج
```

## 📦 النشر إلى Vercel

بعد التأكد من ضبط متغيرات البيئة على Vercel:

```bash
vercel deploy --prod --yes --token $VERCEL_TOKEN --name agentic-0c6ecf62
```

تحقق من الإطلاق بعد بضع ثوانٍ:

```bash
curl https://agentic-0c6ecf62.vercel.app
```

## 📄 الترخيص

مشروع مفتوح المصدر تحت رخصة MIT.
